# vibedev plan

## Goal
Build a hello-world Flask application that exposes a single `/hello` route returning a JSON response. Include dependencies and a short README so the app can be run locally.

## Tasks
- [x] Create app.py with a Flask app exposing GET /hello returning JSON
- [x] Create requirements.txt pinning Flask
- [x] Verify /hello returns the expected JSON via Flask test client
- [x] Write README.md with run instructions

## History
- 2026-05-24 — Initial request: build a hello-world Flask app that returns JSON from /hello
